import { StyleSheet } from 'react-native'

const general = StyleSheet.create({
  input: {
    borderColor: 'gray',
    borderWidth: 1,
    marginHorizontal: 60,
    borderRadius: 5,
    padding: 10,
    marginTop: 10
  }
})

export default general
